// You can use this kind of abstraction to make your code cleaner.
// But remember: never use 'react' or 'table' in the same file your component is.

import { react } from "bunsai/react";
import Test from "./test";

export default react(Test);
